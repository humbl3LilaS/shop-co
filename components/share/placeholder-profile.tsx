import { User } from "lucide-react";

const PlaceholderProfile = () => {
    return (
        <div className="aspect-square w-10 flex items-center justify-center bg-gray-100 rounded-full">
            <User className="size-6" />
        </div>
    );
};

export default PlaceholderProfile;
